# VPS Audit — August 14, 2026

Baseline audit captured on the production VPS before application setup.

## Host

- Hostname: `171545.ip-ptr.tech`
- OS: `Ubuntu 24.04.4 LTS`
- Kernel: `6.8.0-111-generic`
- Virtualization: `KVM`

## Resources

- CPU: `2 vCPU`
- RAM: `2.9 GiB total`, about `328-335 MiB used` at audit time
- Disk: `/dev/vda2`, `20G total`, about `3.0G used`, `16G available`
- Swap: `/swapfile`, `512 MiB`, `0 used`

## Network Exposure

Open listeners observed:

- `0.0.0.0:22/tcp` via `sshd`
- `[::]:22/tcp` via `sshd`
- local resolver only on `127.0.0.53:53` and `127.0.0.54:53`

Ports specifically checked for the future stack:

- `80`: not listening
- `443`: not listening
- `3000`: not listening
- `5432`: not listening

## Running Services

Key active services at baseline:

- `ssh.service`
- `cron.service`
- `dbus.service`
- `qemu-guest-agent.service`
- `rsyslog.service`
- `systemd-networkd.service`
- `systemd-resolved.service`
- `systemd-timesyncd.service`
- `unattended-upgrades.service`

No application stack services were present at audit time.

## Installed Software

Present:

- `git 2.43.0`

Not installed at audit time:

- `nginx`
- `apache2`
- `node`
- `npm`
- `pnpm`
- `psql`
- `postgres`

## Firewall

- `ufw status`: `inactive`
- `systemctl is-active ufw`: `active`

The service exists, but firewall rules are not enabled yet.

## Initial Conclusion

The VPS is effectively a clean Ubuntu host with SSH access only. That matches the expectation for a safe P1 bootstrap target and leaves room to install:

- Node.js + pnpm
- PostgreSQL
- Nginx
- systemd app service
- Let's Encrypt
