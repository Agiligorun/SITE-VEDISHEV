# P2.5 Content Migration Report

Дата: `2026-08-16`

## SOURCE

- `http://advokat-vnp.ru/` открывается по HTTP и отдаёт основной исторический контент.
- `robots.txt` и `sitemap.xml` на старом домене не найдены (`404`).
- Fallback через архив не потребовался на этапе аудита: ключевые страницы, статьи, список книг и библиография доступны напрямую.

## FOUND

- главная / биографические страницы: `7`
- категориальные страницы: `4`
- полнотекстовые авторские статьи: `3`
- библиографические списки публикаций: `2`
- страницы реальных дел / судебных актов: `4`
- видео-страницы: `2`
- legacy contact page: `1`
- исторические изображения: минимум `7`

## IMPORTED

Репозиторий подготовлен к импорту:

- `PracticeAreas`: schema расширена, реальные направления сформированы.
- `Services`: schema расширена, реальные сервисы сформированы.
- `Posts`: добавлены `excerpt`, `authorName`, `practiceAreas`, provenance-поля.
- `Publications`: добавлены библиографические поля и provenance-поля.
- `Books`: добавлены `authors`, `pageCount`, `ISBN`, `fullTextStatus`, provenance-поля.
- `Cases`: добавлены поля для категории, даты акта, процессуальной проблемы, связанной публикации и provenance.
- `Videos`: добавлены provenance-поля.
- homepage blocks: добавлен `booksShowcase`, чтобы книги можно было выводить без ломки P2-layout.
- script: создан [scripts/import-legacy-content.ts](/abs/path/C:/Users/user/Documents/ChatGPT/VEDISHEV/scripts/import-legacy-content.ts) для локального импорта legacy-контента в Payload.

## PUBLISHED

Подготовлены к publication-first на главной:

- real hero positioning;
- trust strip на подтвержденных фактах;
- реальные practice areas;
- биографический about-block;
- published-ready articles, publications, books в рамках import script.

Фактическая запись в локальную БД не была выполнена в этом сеансе, потому что локальный PostgreSQL на `127.0.0.1:5432` недоступен.

## DRAFT

После запуска импорта как `draft` должны остаться:

- `cases` из судебных актов до ручной проверки анонимизации;
- `videos` до редакционного решения о frontend-представлении;
- часть публикаций сверх homepage-featured набора;
- спорные награды и статусы без независимого подтверждения.

## CONTACTS

Legacy-контакты вынесены в [contact-verification.md](/abs/path/C:/Users/user/Documents/ChatGPT/VEDISHEV/docs/content/contact-verification.md) и не должны публиковаться автоматически.

## IMAGES

- найдены реальные исторические фотографии на старом сайте;
- временные кандидаты для profile/hero определены;
- качество ограничено старыми исходниками и требует дальнейшего выбора owner-approved portrait.

## MISSING

- локальный import в Payload не выполнен из-за отсутствующего локального Postgres;
- новые frontend-роуты для `publications`, `books`, `cases`, `videos` пока не создавались;
- owner-verification по контактам отсутствует;
- спорные награды ещё не подтверждены независимыми источниками.

## EXTERNAL VERIFICATION

Использованы внешние источники для обновления и проверки фактов:

- официальный профиль МГКА с реестровым номером `77/1988`;
- новости МГКА и ФПА по новым книгам;
- библиотечные карточки по более поздним монографиям.

Выявлено расхождение:

- исторический сайт и профиль МГКА подтверждают `кандидат юридических наук`;
- отдельные новости 2025-2026 годов называют Николая Павловича `доктором юридических наук`;
- до дополнительной проверки это расхождение должно оставаться в provenance, а не превращаться в production-факт.
